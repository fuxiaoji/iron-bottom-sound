# 《铁底湾的回响 IV》项目治理

> 本文件是**唯一的权威治理文件**。所有贡献者与编码 agent 在改动本仓库前必须先读并遵守本文件（入口见 `AGENTS.md`）。
> 论文流水线线另有三份同级文件：`MEGA_PROMPT.md`（25 阶段总纲）、`RESTRICTS.yaml`（可机读约束清单）、`PROGRESS.md`（阶段台账）。三者冲突时以本文件的「权威与证据」条裁定。

---

## 一、权威与证据

1. 规则冲突优先级固定为：想定特例 > 舰船记录/玩家辅助表 > 通用规则。
2. OCR 和文本抽取仅用于检索；表格、地图、炮位与射界必须回看原页或原图。
3. 禁止猜测缺失的表格单元、修正、优先级或取整方式。不确定项必须写入 `docs/rules/open_questions.md`，并阻止对应自动裁决。
4. 每条可执行规则必须具有稳定 `rule_id`、结构化数据、引擎调用点、事件轨迹和来源边界测试。

**扩展条款（论文线）**：

5. 论文中的数值、文献与结论必须可追溯到本仓库的冻结证据或真实外部来源。**编造数字、编造文献、把未运行的实验报告为已运行**，一律按 `RESTRICTS.yaml` §5 判为 **CRITICAL FABRICATION**，强制退回实验阶段。
6. 失败分支与未触发项必须保持失败/未触发标记，**不得报为通过**；"任务完成"不等于"科学结论成立"，两者不得互相替代。
7. 未分辨结果、域外状态、负结果与零漂移必须保留并显式标注。

---

## 二、模块边界

- `resources/originals/` 只保存不可修改的规范来源副本。
- `resources/derived/structured/` 保存可审查的规则、舰船、地图和想定数据。
- `backend/src/iron_bottom_sound/engine.py` 是裁决入口；UI、API、存储和 LLM 不得复制规则常量。
- UI、LLM 和未来强化学习环境只通过观察、合法行动和命令接口影响游戏。
- 对手隐藏单位和秘密计划不得进入非所属方的 API 响应、WebSocket、DOM 或日志。

---

## 三、变更与验证

- 开始工作前更新 `plan.md`；完成批次后向 `havedone.md` 追加日期、测试、证据和提交哈希。
- 二进制来源使用 Git LFS；源码、YAML、CSV、审计和测试使用普通 Git。
- 规则变更必须运行来源测试、单元测试、确定性回放测试和相关端到端测试。
- 不得把 API 密钥、访问令牌、用户秘密或模型私有推理写入仓库或事件日志。

---

## 四、会话纪律（每轮必须遵守的读写时序）

### 4.1 会话开始（读）

按顺序读取，不得跳过：

1. `agent.md`（本文件）—— 治理宪法。
2. `todo.md` —— 当前活动任务。
3. `PROGRESS.md` —— 流水线阶段台账 + 循环版本登记表（看末尾状态）。
4. `MEGA_PROMPT.md` §2（阶段与门控循环）与 `RESTRICTS.yaml`（复习约束）。
5. `memory/MEMORY.md` —— 长期记忆索引；按需展开命中的正文文件。

> 若本次工作属于某个具体阶段，额外读该阶段在 `docs/pipeline/STAGE_PROMPTS.md` 的提示词模板。

### 4.2 会话结束（写）

1. **完成的任务** → 证据写入 `havedone.md`（日期、测试命令、证据路径、提交哈希）；随后从 `todo.md` 活动区移除。
2. **阶段推进/循环** → 在 `PROGRESS.md` 登记：产物摘要、版本号、变化点、是否触发 REFINE/PIVOT；循环必须同时登记到 §0.1 循环表。
3. **新事实** → 写入 `memory/`（见 §五），并在 `memory/MEMORY.md` 补一行索引。
4. **未完成项** → 留在 `todo.md`，写清阻塞原因；**不得静默丢弃**。

### 4.3 会话中

- 每完成一个阶段，**立即**在 `PROGRESS.md` 登记，不要攒到最后。
- 每个 stage 结束时**重新检查**并核对门控；原文要求**至少循环两遍**，这不是线性流程。
- 门控（阶段 5、9、20）未通过或其前置未满足时，**不得**启动下游阶段的"完成"标记。

---

## 五、长期记忆制度

- **正文层**：`memory/`（随 Git 版本化）。每文件一个事实，frontmatter 含 `name` / `description` / `metadata.type`；`type` ∈ `user` | `feedback` | `project` | `reference`。
- **索引层**：`memory/MEMORY.md`，一条记忆一行，不放正文。
- **自动加载层**：ZCode 用户级记忆 `~/.zcode/cli/memories/projects/seawar-*/memory/` 每轮会话自动进上下文；其中只保留指向本仓库 `memory/` 的**指针**与最新钩子，避免与正文层产生两份互相矛盾的副本。
- **写法**：`feedback` / `project` 类必须写 **Why** 与 **How to apply**；正文内用 `[[name]]` 链接相关记忆。
- **不写**：仓库已能自证的事实（代码结构、git 历史、本文件已写的规则）不重复记录。
- **纠错**：发现记忆已不成立（文件、函数、参数已变）→ 先验证再更新或删除，**不得让过期记忆继续影响决策**。

---

## 六、子 agent 分工、模型选型与审计（强制）

### 6.1 分层委派

| 层 | 工作类型 | 建议执行者 |
|---|---|---|
| 低风险 | 只读检索与定位、文件/词数/字段统计、格式转换、提示词模板实例化填空、批量文本抽取 | 免费/便宜模型（用户指定 glm4.7 / glm4.6v 等） |
| 中风险 | 代码生成、实验脚本改造、图表绘制 | 会话主模型，或复杂模型 |
| 高风险 | 进入论文的**数值**与**文献条目**、定理与证明、证据-主张一致性裁定、门控 verdict、PROCEED/REFINE/PIVOT 决策 | **主 agent 亲做**；困难项可用复杂模型做**多 agent 交叉验证** |

判定标准只有一条：**产出能否被主 agent 用一条命令或一次文件比对独立复现**。能，则可委派；不能，则不可委派。

### 6.2 审计（每次委派必做，不得跳过）

1. 要求子 agent 返回**可核查的原始产物路径或命令输出**，不接受只给结论。
2. 主 agent **亲自重跑或抽样复核**关键数字（重新统计、重新 `grep` 计数、重新解析 JSON、重新编译）。
3. 与既有冻结证据（`research/final_v14/` 的 freeze / hash 文件）比对；任何不一致都要追到差异来源。
4. 审计结果与**未通过项**一并写入 `havedone.md`——**不得只记录通过的部分**。
5. 文献类产出必须保留原始 `cite_key` 与 DOI，按 `RESTRICTS.yaml` §5 `literature_fidelity` 复核；无法溯源的一律剔除。

### 6.3 隔离

- 阶段 14（`RESULT_ANALYSIS`）与阶段 24（`3RD_PARTY_REVIEW`）**必须使用独立上下文**，且不得把主 agent 的既有结论喂进去诱导附和。
- 不相关的独立任务应在**同一条消息里并行派发**，不要串行等待。

### 6.4 现状提示

模型路由与可用通道的**实测状态**见 `memory/agent-delegation-environment.md`。若该记忆所述前置（GLM 端点与 key、客户端模型映射）尚未满足，则"用免费模型跑子 agent"**不具备执行条件**，此时应继续走 ZCode 内置 agent，并**提高**而非降低审计强度。

---

## 七、论文流水线纪律

1. 严格按 `MEGA_PROMPT.md` 的 25 阶段 / 9 阶段组执行；阶段提示词以 `docs/pipeline/STAGE_PROMPTS.md` 为唯一来源。
2. 严格执行 `RESTRICTS.yaml`，并在以下时机**强制复习**：会话开始、阶段 9 前、阶段 16/17 前、阶段 20 与投稿前自查。
3. 真实性与资源红线（摘要）：**禁止**用随机数伪造 Loss 曲线或实验结果；**必须**实现真实收敛准则；遇 NaN/Inf **必须追根因**，禁止用 `try-except` / `np.nan_to_num()` 掩盖；主实验前**必须**先跑 1 条件 Pilot 并打印 `TIME_ESTIMATE:`；代码**必须**含 `time_guard`，在预算 80% 时保存已收集数据并停止。
4. 撰写红线：**1–2 个核心创新点**（Sushi, not Curry）；**图 1 必须能独立传达核心贡献**；**强制消融**——缺消融直接拒绝进入下一步；基线须**同等精力调参**；字数不足只能补实质内容，**严禁车轱辘话凑字数**。
5. 环境规范：优先 stdlib / numpy / math / statistics；NumPy 2.x 兼容（`np.trapz`→`np.trapezoid`、`np.erfinv`→`scipy.special.erfinv`、`np.bool/int/float`→原生类型、`np.math`→`math`）；环境配置必须可逆并记入 `PROGRESS.md`。
6. 环境可逆性：优先 `.venv` / conda / Docker；不得对系统 Python 做不可逆改动（本仓库已有 `.venv`）。

---

## 八、红线清单（触碰即中断当前阶段并自我修复）

| # | 红线 | 判定 |
|---|---|---|
| R1 | 用 `random.uniform()` 等伪造实验数据或下降曲线 | CRITICAL |
| R2 | 无真实收敛准则（固定循环冒充收敛） | CRITICAL |
| R3 | 用 `try-except` / `np.nan_to_num()` 掩盖 NaN/Inf 而不追根因 | CRITICAL |
| R4 | 论文数值/论断与 log、results 不一致（含试验次数不符） | CRITICAL FABRICATION |
| R5 | 论文提到"有效组件"却无消融实验 | 拒绝进入下一步 |
| R6 | 编造或无法溯源的文献 | 剔除并回溯 |
| R7 | 密钥、令牌、用户秘密、模型私有推理入库或入日志 | CRITICAL |
| R8 | 把失败分支/未触发扩展写为通过 | CRITICAL |
| R9 | 把环境搭建、依赖安装、调试日志当作研究贡献或实验发现 | 按 `topic_constraint` 驳回 |
| R10 | 未审计即采用子 agent 产出 | 停止，补审计 |

# 数据约定

- `IBS-R-<section>-<number>`：规则。
- `IBS-T-<name>`：裁决表。
- `IBS-S-<two digits>`：想定。
- `IBS-U-<side>-<slug>`：舰船。
- 六角格外部标签为 `A1` 至 `HH27`，内部为零基轴向坐标。
- YAML 用于带语义的规则、舰船、想定和地形；CSV 用于密集二维矩阵。
- 每个结构化条目必须包含 `source`、`status` 和稳定 ID。

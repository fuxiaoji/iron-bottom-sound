# v12 protocol frozen before discovery

Date: 2026-09-13. Local prospective protocol, not an externally registered preregistration.
Source: REQUESTED_PLAN.md. No v12 discovery or held-out outcome has been run when this file is written.

Primary held-out grid: geometry={head_on, parallel, crossing}, eta_r={0.85,1.15}, eta_v={0.9,1.1}; all 12 cells retained. Discovery: 3 geometries x eta_r={0.8,1.2}, eta_v=1.0. No optional stress grid. T=6, Grid-3={-1,0,1}; turn bound 60 degrees/turn; LF spacing=2, n_ships=3, n_sub=6. Gamma=1; trapezoid payoff at causal endpoint states. Blue range and speed scaled relative to Red. Frozen kernel: research/results/e01/kernel_fits.json CA.

Primary hypothesis: range-disadvantaged side has greater path-ordered flexibility value, Delta_F=sign(eta_r-1)*(V_CC-V_FF)>0. This equality means Delta_F is algebraically the OLD bilateral sign hypothesis; decomposition alone cannot supply independent evidence. Report both unilateral values, alternative decomposition path, and four values if the extensive-form branch is authorized by gates.

Controls require a proved exchange isometry of the COMPLETE history and capability kernel; equal capabilities alone are insufficient. Test the prescribed three geometries without changing them. Reflect action signs for reflection isometries. No payoff projection onto skew matrices is permitted. Candidate degeneracy at angular sector boundaries is handled by a documented deterministic 1e-10 degree snap in BOTH scalar/vector sector classification, not by altering payoffs to force symmetry.

Phase A exact-history vs off uses identical seeds, payoff semantics and tolerances. Solver audit threshold is min(0.1,0.005*max(1,abs(V))); main threshold=0.01*max(1,abs(V)). Cap=200. Unconverged policies are never included in scientific means. Retain failed states, bounds and iteration traces. Sweep tol={0.05,0.02,0.01,0.005}, cap={15,30,60,120,200}. Diagnostics: 100 reachable states each for swap and independent scalar/vector checks. MC N=200 paired seeds for headline controls; pilot N=8 for profiling only. Bootstrap=10000, seed=12345; seeds start at 120000. A symmetry-paired estimator, if used, is explicitly distinguished from independent MC and is not evidence by itself.

Per-epoch LB/V/UB/gap certify the remaining OPEN-LOOP game only. Sum gaps is a reported local residual diagnostic, NOT a theorem bounding feedback equilibrium or rollout policy error; no sign certification is based on this sum. Exact finite tree values use full backward induction or perfect-recall sequence-form LP, with feasibility/duality residuals. Coarse-game exact means complete finite enumeration up to numerical LP precision, never exact continuous dynamics/game.

Gate: all numerical implementation audits must pass before scientific branch expansion. If correctly specified controls remain significantly nonzero after converged symmetry-preserving policies or independent exact enumeration, permanently remove Commitment. If controls pass, exact Grid-3 six anchors >=5 correct signs selects Branch C; otherwise D/E. Held-out >=10/12 certified correct with all additional robustness gates supports strong; 8-9 or restricted regimes conditional; <8 or multiple robust reversals permanently remove core. Optional F/G are not blockers and G only follows primary success. Do not rerun unrelated prior experiments. A failed hard gate skips dependent experiments by design; it does not make them executed or successful.

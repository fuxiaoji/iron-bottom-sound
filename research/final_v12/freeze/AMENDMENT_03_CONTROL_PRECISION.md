# Control precision amendment 03, 2026-09-13

Timing: before ANY asymmetric Grid-3 discovery or held-out evaluation. All original Phase A results and thresholds remain reported. The original literal Monte Carlo gate is NOT recorded as passed.

The corrected N=200 controls do not all meet the requested absolute sample-mean threshold: head_on C6=0.296 (CI [-0.736,1.370]); parallel C2=0.344 (CI [0.076,0.618]). The policy-mapping defect identified independently was corrected, and the same-seed outputs largely remained unchanged. These facts alone do not separate Monte Carlo fluctuation from residual policy bias. An arbitrary 0.25 threshold on a noisy sample mean is not a bound on its population expectation; requiring every unadjusted 95% interval in nine comparisons to contain zero also lacks family-wise error control.

To resolve this WITHOUT changing seeds, sample size, capabilities, the primary sign hypothesis, or held-out grid, the independent full Grid-3 symmetric controls are moved forward into the correctness audit. This is a disclosed ordering deviation from the requested Phase A-before-B gate, motivated by its own optional deterministic-expectation route. Only the three symmetric controls are allowed; no asymmetric result is consulted. Complete backward induction checks all 531441 leaf pairs at T=6 for h=1,2,3,6. All finite-game values must be within 1e-6 of zero and retain numerical certificates.

If these independent exact controls fail, repair a located implementation error or trigger the plan's permanent stop. If they pass, the original MC gate remains failed as written, while an explicitly amended deterministic correctness gate may permit the exact discovery branch. Do not describe this as literal completion of an unchanged protocol or as proof that a receding policy equals the feedback equilibrium. The receding statistics remain algorithm-specific diagnostics; equilibrium conclusions must use the independent exact finite-game solver. This amendment repairs a control criterion before discovery, and is not an after-outcome change to the scientific comparative-static hypothesis.

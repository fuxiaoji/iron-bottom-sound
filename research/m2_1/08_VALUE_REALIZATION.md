# 08_VALUE_REALIZATION.md — NOT TESTED (Stage C gated)

Stage C (E0/E2/E3 over mechanistically-good vs bad arms) is gated on Stage B.
One value-realization datum already exists from the previous round's gold
run and should anchor the next round's hypothesis:

- UNMASK_BROADSIDE: +0.011 at T0 under E0 (scripted), but **-0.015 under E3**
  (adversarial opponent): a broadside exposes the ship to return fire, and
  only an opponent that punishes exposure makes the tactic net-negative. Any
  future value panel must include an evaluator that responds.

Combined with this round's rule-level results, the working hypothesis for
M2.2 (PI's call) is: **steep rule-level leverage + poor value realization** —
i.e., the platform is rich, and the evaluator family built so far is the
bottleneck (SCRIPTED_FLATTENING family), not the game.

# v14：有限重规划预算的理论、认证优化与完整论文计划

用户批准：2026-09-13。状态：执行中。范围覆盖完整英文论文，不受旧 v13 停在写作前范围约束；v13 FAIL 和历史结果保持不变。

## 一、研究方向与最终交付

主攻数学理论与运筹优化方法，以编队博弈作应用。主线：方向性作用场 → 轨迹积分收益 → 历史依赖策略空间 → 有限重规划预算 → 有保证的优化算法 → 预算配置建议。

最终实验回答：允许多大性能损失时，最少需要几次重规划，应安排在哪里，算法如何证明答案足够接近最优？不依赖速度提高必然增加适应价值。

交付完整英文论文、证明附录、约八张主图四张主表、补充材料、复现包、中文说明、两轮内部审阅及修订回复。以中科院分区定位一区潜力和二区投稿；不能预先保证质量层级或录用。建立逐项台账，旧任务映射为已完成、有新任务承接、原失败分支未触发，不能把未执行写成通过。

## 二、理论与算法

1. 建立主张—假设—证明—实验—图表表。修复旧全状态闭环退化错误，正确表述交换奇对称与对称固定态零值；区分运行速度与可选能力，保留反例。复核需引用的核、积分、历史结果；不可追溯的数据重算，无关章节移至补充。主文无错误证明或循环论证。
2. 更新日程 S 是 {1,...,T−1} 的子集，|S|≤K。初始 t=0 不计预算。日程事先公开，在每个更新点依据已执行历史选择到下一更新点之间的密封行动块；当前行动和未执行后缀对对方保密。非更新时间执行已选块；下一更新时间记得全部已观察历史及自己的旧决策。双方同时选择时互不见当前块。
3. 固定对手 κ∈{F,C}；W_K(κ)=max_{|S|≤K}V(S,κ)。报告前沿、日程、上下界、全灵活损失、最小预算、时间、内存和缺口。仅确定性公开日程，不纳入事件触发、隐藏随机日程或双方同时设计日程。
4. 基础定理：策略嵌入、预算单调性、F/C端点等价；这些不单独计创新。结构分析：次模性及反例/充分条件。候选创新：删更新损失证书、跨日程对手策略复用、有效剪枝与误差终止保证、方向性/历史几何解释。分段角度核不能假设全局光滑。
5. 扩展完美回忆序列形式；小规模完全枚举作为基准。分支定界以可行解为下界、更多更新放松为上界。对手策略只有经可迁移性证明才能跨日程用作上界。预算复用、稀疏存储、断点与全配置缓存；旧 pose-only cache 禁止。每条结果包含 S,K,κ,LB,UB,残差、时间、内存、终止原因、输入哈希，浮点证书与数学证明分开。
6. 扩大实验前对最近相关研究进行逐篇比较：战略信息获取 https://arxiv.org/abs/2102.05469；信息结构排序 https://arxiv.org/abs/2005.06673；信息替代性 https://arxiv.org/abs/1703.08636；博弈抽象界 https://papers.nips.cc/paper_files/paper/2018/hash/aa942ab2bfa6ebda4840e7360ce6e7ef-Abstract.html 及其最近后续。明确新增内容，不能以换名称、枚举最优、预算单调或优于可行基线的恒等事实冒充创新。

## 三、冻结实验与统计口径

所有实验保持 v13 修复后的方向性支付核、gamma=1、默认三船 LF、间距2、n_sub=6、最大转向60度、对手速度6和双方 range=1；焦点速度比例为强制运行速度。除明确消融项外不变。

| 阶段 | 预先指定安排 |
|---|---|
|正确性|T=1,2,3 微型完整策略枚举，任意日程端点、隐藏信息、独立交换|
|开发|三几何×速度{.85,1,1.15}，距离16，Grid3，T6；对手F/C|
|冻结测试|三几何×四配对配置(speed,distance)={(.80,13.5),(.95,15),(1.05,17),(1.20,18.5)}；Grid3 T6，每条件全部32日程，共12物理/24对手条件|
|稳健性|三几何，速度1距离16；Grid3 T4/5/6/7；T4 Grid5/7，对手F/C|
|消融|相同三几何锚点，T4 Grid3：方向性与角度平均、积分与终点、LF与刚性，对手F/C；各消融单独改变一项|
|最终决策|冻结测试集保留90/95/99%适应价值的最小预算、日程、数值证书、策略回放|

新配置运行前检查历史重合并记录，旧结果不能叫未看过的新验证。每个物理实例独立生成交换支付矩阵；每几何开发速度1及冻结测试首个配置独立求解全部镜像日程。主结果不通过直接取负制造镜像通过。

比较：完整枚举、基础分支定界、增强剪枝、均匀/前置/后置/逐步贪心。均匀S={floor(jT/(K+1)):j=1..K}；前置1..K，后置T−K..T−1。相同模型预算精度，分别计预处理与优化成本。报告绝对差、上下界、全灵活损失、90/95/99%最小预算、等缺口时间/调用/内存。适应价值接近0报告绝对损失；确定性实例不伪装随机样本，不机械加p值。全部反向、无差异、超时、未分辨保留。

本机16GB，默认一个重型求解进程，进程树8GB；普通内层20分钟，超时转有上下界续算，每实例累计最多6小时。不能放松精度后报精确。小中规模先完整基准，扩大规模使用证书。错误输出归档并披露修订后重算，不为符号重选参数。

## 四、技能、图表、完整稿件

固定版本K-Dense四skill：scientific-critical-thinking设计/反例；peer-review结构化两轮内部审阅；scientific-visualization真实可访问图表；scientific-writing证据绑定/全文/一致性。保留来源commit与SHA；来源见 ../final_v12/skills/PROVENANCE.json。不是独立期刊评审或分区认证。

约八主图：模型/信息；方向性/积分；预算前沿；最优更新；损失证书；时间/缺口；稳健/消融；容限/最小预算。四主表：符号假设、文献差异、设计、主要结果。所有图均有原数据、代码、图注、PDF和高分辨率PNG；视觉检查负值、缺失、单位、字体、颜色、版面。不能凑数重复证据。

英文LaTeX主文：引言/缺口→模型→预算问题→理论→算法→实验→决策解释/局限→结论。先模型理论算法结果，后摘要标题。反例在相关主张处说明，历史细节附录。交付paper_v14/源码、PDF、证明与补充，中文说明；作者基金声明待作者确认，不编造。

## 五、完成与质量验收

两轮内部审阅并回复修订：第一轮数学/运筹（量词、假设、可行性、证明、误差、文献、公平比较）；第二轮全文/复现（摘要边界、数字图表、引用、PDF逐页）。

理论：完整证明/降级未证命题/处理反例。数值：核心区间有效、未分辨披露、无关键正确性问题。创新：对最近工作明确新增，不能换皮。实验：各阶段有记录结论。论文：全文附录图表引用完整且无核心逻辑问题。复现：reproduce_v14.py、环境、manifest、hash及实际执行日志。投稿：核实scope、最新可核验中科院分区及作者要求，不能核验就注明。

结束必须交付完整研究包、完整论文、逐项完成台账，不能止步跑完实验或开写前。最终分别评价工作完成、数学数值可靠、一区竞争力、二区不足；创新或证据不足不准写成一区已认证。

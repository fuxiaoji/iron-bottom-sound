# V13.1 reproducibility — experimental packet only

Run from the repository root. Baseline commit: `fc77a65e970e92cca429e6c87498b0c68a70bf1e`; branch: `codex/v13-1-mobility-flexibility`. Experimental files are uncommitted; the baseline commit alone does not contain this packet. `FINAL_MANIFEST.json` and the frozen source hashes identify the actual delivered source and data. No Git commit or remote publication is claimed.

## Normal verification

```sh
.venv/bin/python reproduce_v13.py
.venv/bin/python reproduce_v13.py analyze
```

The first command verifies the packet checksums and prints the recorded gate. The second first verifies the packet and then recomputes the frozen interactions, descriptive mechanism associations, numerical/archival audits and source tables. Neither starts new scientific experiments or writes a paper. Float values and JSON serialization may differ across Python/NumPy/SciPy versions; use the recorded environment for a byte-for-byte comparison.

## Environment

`ENVIRONMENT.json` records the executed environment: Python 3.14.6, NumPy 2.5.3, SciPy 1.18.1, Matplotlib 3.11.1, pytest 9.1.1, Pillow 12.3.0, macOS arm64. HiGHS is the SciPy LP backend. Poppler `pdfinfo`/`pdftoppm` supplies PDF metadata/render checks; pypdf is not installed in the scientific venv. These are observation records, not claims of cross-platform reproduction.

## Original amended computation commands

These commands document the finished run. **The scientific Gate is FAIL; do not launch new parameter expansion.** Existing complete results are read from disk with configuration guards. Fresh numerical reproduction should use a separate copy containing the same source, inputs and protocol files, with a separately archived/empty set of output directories; preserve all final and pre-amendment artifacts in this packet.

```sh
.venv/bin/python -u -m research.experiments.v13_exact --phase discovery --workers 3
.venv/bin/python -u -m research.experiments.v13_exact --phase confirmatory --workers 3
.venv/bin/python -u -m research.experiments.v13_mechanism --phase discovery --workers 2
.venv/bin/python -u -m research.experiments.v13_mechanism --phase confirmatory --workers 2
.venv/bin/python -u -m research.experiments.v13_capability
.venv/bin/python -m research.experiments.v13_analysis
.venv/bin/python -m research.experiments.v13_audit
.venv/bin/python -m research.experiments.v13_figures_delivery
.venv/bin/python -m research.experiments.v13_report
.venv/bin/python -m research.experiments.v13_review
```

Figures regenerated by Matplotlib can have different PDF creation timestamps even with identical visual content. Final PNG/PDF hashes identify the inspected delivery files. Report assembly asserts the failed branch and does not synthesize a successful gate.

The delivery figure wrapper retains the original frozen plotting source and moves only the C/D legends outside the data region after visual inspection identified overlapping marks. It changes no data, estimates, axes definitions or selection rules.

Targeted regression command (30 tests, already executed):

```sh
.venv/bin/python -m pytest -q tests/test_commitment_v12.py tests/test_exact_v12.py tests/test_v13_regression.py tests/test_tracking_v13.py tests/test_capability_v13.py tests/test_payoff_boundary_v13.py
```

`REGRESSION_FINAL.log` is the original frozen output; the command exited zero. A test count is a local validity check, not proof of universal solver correctness or manuscript merit.

## Data and numerical meaning

- Main physical game: three LF vessels, offsets 0/2/4, six integration substeps, T=6, normalized controls −1/0/1 multiplied by 60°, range ratios 1, Red speed 6, Blue speed 6 times its declared ratio. Kernel fit: `research/results/e01/kernel_fits.json`, CA-labelled entry, target-speed channel remains fixed (wrapper clips the supplied 6 to 5). No damage-state updates or empirically calibrated vessel dynamics are inferred.
- Payoff: undiscounted causal endpoint trapezoid, nine ship pairs per epoch. Boundary numerical completion is stated in `IMPLEMENTATION_AMENDMENT_01.md`; no payoff skew projection. Terminal matrices contain all 729×729 history pairs per main physical game, with four information-structure values. The full sequence-form information law observes executed public actions/history; current sealed actions and unexecuted suffixes remain hidden.
- Discovery: three geometries × five speeds, plus independent mirrors. Confirmation: three geometries × four held-out speeds, plus independent mirrors. Each physical cell is independently solved; parallel exchange additionally reflects signed turns. Mirror checks do not double the endpoint denominator.
- Bounds are finite-game numerical saddle/duality intervals from floating-point payoffs and LP precision, not rational interval-arithmetic proofs, population confidence intervals, continuous-control bounds or feedback guarantees from local open-loop optimization. Reported gaps do not include model calibration error, grid error or all possible geometry roundoff.
- Capability: low absolute menus {.75,1}, high {.75,1,1.3}, opponent {1}, T=4; common initial prehistory. The low action subtree is exactly a subset of the high game. Complete rectangular sequence form and DP check both focal sides. Six interactions stay separate from the twelve primary endpoints.
- Mechanism: 32 probe seeds 2026091300…2026091331, PCG64, two independent turn sequences per probe; same sequences shared across speeds/geometries. The fixed relative domain has 5280 states (440 positions × 12 headings); see the protocol and stored `field_domain_points` for direct verification. Embedded heading distance uses length 4. Two alpha levels, per-episode medians followed by per-cell medians. Source tables include probe quartiles, negative correction, zero drift and domain-exceeding frequencies. No iid N=12 mechanism inference.

## Provenance and scope

Original discovery/mechanism/confirmatory design freezes remain immutable. `pre_boundary_amendment/` retains original numerical outputs, old changed-source snapshots and hashes; the first confirmation failed. `RERUN_FREEZE.json` was created before amended output evaluation. `analysis/AMENDMENT_COMPARISON.csv` compares all available original values/metrics and `AMENDMENT_PAYOFF_COMPARISON.csv` compares payoff matrices. Missing original parallel validation speeds are not imputed.

The v12/fallback freeze has its own 429-file manifest. The final audit checks it, the original-output archive, the rerun freeze and the original design. No old pose-only cache, new full v12 Monte Carlo audit, manuscript rewrite, submission or external message is part of this delivery.

Review and visualization skill outputs are local structural checks with disclosed limits, not independent reviewer endorsement, journal compliance or CAS tier certification. External mathematical novelty and target-journal reviews remain future author work if a manuscript is later developed.

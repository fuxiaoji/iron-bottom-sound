# M2_0_DISCOVERY_FREEZE.md

**Not applicable in the planned sense**: the plan reserves the freeze document
for surviving tracks entering confirmatory. No track survived discovery.
Recorded here instead are the frozen knobs that WERE used, so any re-analysis
of the same data is bound to them:

- snapshot set: 60 movement snapshots (30/scenario), (profile-pair, seed, turn)
  order, A-MECH-03 cap;
- macro vocabulary: HOLD / STRAIGHT_SLOW / STRAIGHT_FAST / TURN_PORT_60 /
  TURN_STARBOARD_60 / LEADER_PROPOSAL;
- value: H=1 scripted continuation, utility = terminal (±1/0) or VP-swing/10
  clipped (VALUE_DEFINITION.md), 5 CRN replicates for reported values, rep=0
  for macro selection (A-MECH-02), independent per-group macro choice
  (A-MECH-01);
- epsilon = 0.05 normalized; tie tolerance 1e-9;
- opponent: scripted TacticalCommander, same profile pair as source match;
- Track B: 150-match census (B0); B1 paired hierarchy variants (axis-side only,
  F6); B2 7 organizations x 36 cells x 5 reps.

# figures/

No track was run, so there are no track figures to plot. Deliberately empty rather
than filled with positive-control curves, which are tabulated in
`raw/positive_controls.json` and reproduced in `01_ENVIRONMENT_POLICY_AUDIT.md`.

When Tracks A/B/C run, the pre-registered figures are: (A) Δ(s,B) distribution and
the gain-concentration curve; (A) allocation comparison at equal total budget;
(B) boundary recall vs K by acquisition strategy; (C) attribution accuracy vs query
fraction.
